apiVersion: batch/v1
kind: Job
metadata:
  name: cpsecret
  namespace: smq-dev
spec:
  template:
    metadata:
      name: cpsecret
    spec:
      serviceAccountName: sqm-sa
      containers:
      - name: kubectl
        image: bitnami/kubectl
        command:
         - "bin/bash"
         - "-c"
         - "kubectl get get secret ibm-entitlement-key -n openshift-operators -o json | jq -r '.metadata.namespace=smq-dev' | oc apply -f -"
      restartPolicy: Never 